# Contributing

1. Create a focused feature branch.
2. Keep physics logic independent from Streamlit.
3. Add or update tests for every behavior change.
4. Run `uv run pytest` and `uv run ruff check .`.
5. Use Conventional Commits, such as `feat:`, `fix:`, `docs:`, or `test:`.

Keep the interface student-friendly and document every scientific assumption.
